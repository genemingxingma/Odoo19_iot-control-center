# V2 隔离验收记录

日期：2026-09-06。候选版本：Odoo `19.0.2.0.0`、桥接协议 `2`、继电器固件 `2.0.0`。

## 环境边界

- 实测服务器为 `imytestth`，使用其 Odoo `19.0+e-20260108` 运行时。
- 回归库：`iot_v2_isolated_20260906`；升级演练库：`iot_v2_migration_20260906`。
- 候选源码、数据目录与日志均在 `/opt/odoo/iot-v2-isolated-20260906` 下，与生产目录分离。
- 使用合成数据，没有复制患者、生产考勤或其他生产业务记录。禁用测试进程的 cron 和真实设备下发配置。
- 浏览器验收只经 SSH 隧道访问回环地址。自动测试端口与预览端口分开，避免浏览器长连接延迟测试进程退出。
- 没有升级生产数据库、替换生产中间件、重启生产服务或向真实设备推送固件。

## 已验证

| 项目 | 结果 |
| --- | --- |
| Odoo 模块回归 | 34 项，0 失败、0 错误，进程退出码 0 |
| 独立协议解析测试 | 7 项通过 |
| Rust 持久队列及 HTTP 回执测试 | 4 项通过；确认前保留、重启读取、永久拒绝归档、错误回执拒绝、临时失败与永久失败区分 |
| 固件编译 | ESP8266 1MB/DOUT 配置成功；RAM 34168 / 81920 字节，Flash 471375 / 958448 字节 |
| 翻译静态检查 | 788 个源词条，4 份语言目录通过 |
| 运行时字段翻译 | 探头名称的中文、泰文、英文实际 ORM 元数据正确 |
| 源码与视图 | 60 个 Python 文件 AST、37 个 XML 文件解析通过；JavaScript 语法及 Git 空白检查通过 |

升级演练先安装 V1 并建立监测原始行、小时汇总行、合成员工与考勤。未开启授权开关时，V2 升级失败且两条旧监测记录仍在；通过 ORM 开启测试授权后，升级成功，旧温湿度历史被清空，员工、考勤、联系人、网关和探头名称仍在。

随后通过真正的新事务入库服务发送事件、重发事件、使用同一事件编号修改内容：最终只有一条原始记录和一条事件回执，修改内容的重放被拒绝。

网页验收使用两个具名探头、每个探头 24 个小时采样的合成数据。已检查列表与图例的名称及编号、小时图与原始记录的时区一致性。现场发现服务器默认小时标签可能使用无上午/下午标记的 12 小时制，导致图表按标签合并不同时段；候选已改为日期、24 小时制和 UTC 偏移的明确标签。

双指标图的左右纵轴与原始数值已核对。指标菜单只保留温度和相对湿度，极值及样本数保留在透视统计中；没有把不同单位相加或用温湿度占比制作饼图。

## 发布前仍需完成

- 每种真实继电器硬件型号的 GPIO 极性、断电重启、倒计时到期、手动关闭、重放、看门狗和网络切换测试。编译通过不能替代电气状态验证，紫外设备仍需物理联锁。
- Linux 中间件发布构建及实际 MQTT 断线、磁盘满、进程中断的故障注入，使用与生产相当的数据量进行压测。
- 生产备份恢复演练、设备身份清单与公司归属审查、OTA 证书信任和 OpenWrt 主机密钥准备。
- 告警确认/处理工作流、带回差和持续时间的告警策略、自适应保峰降采样，以及更完整的队列容量/年龄诊断尚未实现，不能把本次核心架构验收等同于这些功能已完成。

本记录是候选验收证据，不是生产或全量固件发布批准。发布与回退步骤见 `UPGRADE_V2.md`。
