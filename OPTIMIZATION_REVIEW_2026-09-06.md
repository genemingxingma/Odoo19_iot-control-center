# IoT 控制中心优化审查

日期：2026-09-06。审查基线：`36ce015`，分支 `codex/th-monitoring-1.0.19`，模块版本 `19.0.1.0.19`。

本次审查覆盖温湿度采集与展示、继电器和公司网络配置、Rust 中间件、OpenWrt 心跳、考勤处理及测试维护的主要代码路径。没有登录服务器、操作真实设备、升级固件、修改业务数据或部署。以下不是生产故障已经发生的断言，也不是完整的运行环境安全审计。

## 结论

当前最有价值的工作不是更换语言或重写整个模块，而是补齐可靠性边界：真正入库后确认、补传幂等、租户身份隔离、可合并的历史汇总、可确认的配置版本和明确的关闭命令。

保留 Odoo 管理业务与权限、Rust 处理常连接和设备协议的分工。继续坚持国家信息来自公司配置、公司内网地址由控制中心下发并由设备保存，不能在固件中写死某家公司的网络地址。

## 优先处理的问题

### 1. P1：入库失败可能被确认成成功

- 位置：`services/tcp_service.py:266`、`:331`；`controllers/internal_ingest.py:88`、`:94`；`middleware/iot_bridge/src/main.rs:1442`。
- 二进制帧处理捕获所有异常后只记日志，不向调用方传递失败。控制器随后返回 `ok: true`；中间件把任何 2xx 当作成功，不会将这次失败重新入队。
- 已隔离复现：执行实际方法，输入校验正确的帧，让入库方法抛出异常，外层仍正常返回。
- 建议：明确区分成功、永久拒绝和暂时失败；只有事务提交成功才确认；数据库暂时失败返回可重试错误；坏帧进入可审计的拒绝计数或死信，不无限重试。
- 验收：注入数据库不可用和提交失败，样本应留在待确认队列；恢复后只入库一次。

### 2. P1：持久队列在发送确认前删除消息

- 位置：`middleware/iot_bridge/src/main.rs:1036`、`:1076`、`:1104`。
- `take_batch(200)` 先把未取出的内容写回队列文件，再返回取出的消息供 HTTP 转发。这一批消息已只存在于内存；期间进程被杀或服务器重启，会丢失未完成的消息。重新入队只覆盖正常返回的发送失败，不覆盖进程死亡。
- 每取 200 条都读取并重写剩余整个文件，还会在异步任务中执行同步文件操作；积压越大，恢复越慢。
- 证据级别：源码调用顺序确认，未进行实际断电或进程故障注入。
- 建议：采用带确认/租约的持久队列，例如 SQLite WAL，或可恢复的追加日志加确认游标；发送成功后才确认删除。加入容量、最老消息年龄、重试次数、磁盘异常和死信指标。
- 验收：在取消息、发送前、提交后响应丢失等位置反复终止进程，重启后消息不丢失，消费端幂等。

### 3. P1：二进制补传改变采集时间，当前去重键不够用

- 位置：`middleware/iot_bridge/src/main.rs:1281`；`services/tcp_service.py:321`。
- 中间件转发内容没有固定的接收时间或事件 ID；服务端每次处理同一帧都重新使用当前时间。协议序列号也没有参与事件身份计算。
- 已隔离复现：同一帧在两个时间处理，读数相同但 `reported_at` 不同。因此按探头和时间戳去重不能防止这种重试重复；离线积压还可能被画到恢复网络后的时间，影响最新状态与告警判断。
- 建议：中间件第一次接收时生成稳定事件 ID 和接收时间，所有重试保留原值；设备采样时间、网关接收时间、入库时间分开存储。8 位序列号会回绕，不能单独作为永久唯一键。
- 验收：同帧响应丢失、离线后补传、乱序、序号回绕、设备重启，均不重复计数、不伪造“刚采集”的时间。

### 4. P1：探头身份没有公司/网关边界

- 位置：`models/th_sensor.py:71`；`services/tcp_service.py:95`。
- 数据库唯一约束只有 `(node_id, probe_code)`；查找在 `sudo()` 下仅使用这两个字段。命中已有探头后，会直接把 `gateway_id` 改成当前网关。
- 已隔离复现：另一个网关送来相同编号和通道，命中原公司的探头，并改写其网关归属。真实串记录的前提是设备短编号发生碰撞；没有检查线上是否已经发生。
- 建议：定义稳定的设备注册身份与经过认证的租户/网关归属；跨网关漫游应有明确策略，不允许仅凭上报短编号自动迁移。按选定身份方案做数据迁移和约束，不能只在一个查询中补公司条件。
- 验收：两家公司使用相同短编号和通道仍形成独立历史、告警和权限边界；合法换网关需要受控重绑定。

### 5. P1：补入旧数据可能覆盖已有日汇总

- 位置：`models/th_reading.py:318`、`:338`、`:378`、`:387`。
- 汇总计算排除已有日汇总，但随后删除并替换该日汇总。已经压缩完成的一天只要后来出现一条旧原始记录，就可能用这一条记录替代原先整天的统计。
- 已隔离复现：运行实际聚合和删除 SQL 的 SQLite 方言适配版本；原汇总为 100 个样本、均值 10，新补一条 20 后，替代值为 1 个样本、均值 20，而不是 101 个样本、均值约 10.099。
- 同一 SQL 还会在当天存在任意原始记录时排除当天所有小时汇总；迁移产生部分小时汇总与部分原始记录共存时也需要测试。
- 建议：在事件去重基础上，原子合并互不重叠的新旧样本统计，或使用独立汇总表及可重算来源；汇总保留样本数、加权和、极值、时间范围和版本。不能把有重叠的原始数据与汇总简单相加。
- 验收：日汇总后二次补传、跨午夜、部分小时汇总、重复运行及中断恢复均保持计数和极值正确。

### 6. P1：公司网络配置确认不完整

- 位置：`models/iot_device.py:938`、`:963`、`:1003`、`:1084`；`models/res_company.py:19`。
- 只要上报的当前 MQTT 主机与期望主机相同，就清除 `network_config_dirty`；没有确认端口、备用连接、OTA 地址和配置版本。
- 已隔离复现：期望端口已改变，但仅上报相同主机名的旧状态，仍会清除待同步标记。如果该状态在定时同步前到达，修改可能不再被重试下发。
- 设备 `company_id` 改变的写入路径本身也没有触发网络配置重新同步，需要覆盖迁移公司和解除绑定的生命周期。
- 建议：公司生成单调配置版本及内容摘要；设备持久化完成后回报已应用版本；完整匹配才清除待同步。公司变更时重新生成目标配置；解除绑定时安全清理旧公司的内网配置，保留可控恢复路径。
- 验收：仅改端口、仅改 OTA 地址、切换公司、离线恢复、公网回退、设备重启都能确认正确版本，没有旧公司配置残留。

### 7. P1 安全设计改进：延时模式不能拦截明确关闭

- 位置：`models/iot_device.py:583`；`firmware/esp8266_relay/esp8266_relay/esp8266_relay.ino:810`。
- Odoo 的普通关闭入口和固件的普通继电器命令都会在延时模式中拒绝执行。固件确实另有 `delay_cancel`，其实现会关断继电器；因此不能据此声称设备完全无法关闭，也不能认定这就是历史紫外灯常亮的唯一原因。
- 建议：明确的紧急 OFF 应高于延时和排程，取消当前延时并关断，返回执行确认；定义是否保持安全锁定以及如何人工解除，避免下一次排程马上重新开启。
- `delay_toggle` 是反转操作，不适合断网重试或作为紧急停止。可改为幂等的 `delay_start` / `delay_cancel`，并带命令 ID 和过期时间。
- 验收：先在模拟设备验证延时中关闭、重复命令、过期命令、断网与恢复；真实紫外设备测试须有现场人员和物理隔离。本次没有发送任何设备命令。

### 8. P2：部分统计接口存在确定的运行错误，聚合口径也不一致

- 位置：`models/th_reading.py:261`；`static/src/js/th_reading_graph_lock.js:236`；`models/th_sensor.py:192`。
- `read_group(self, domain, fields, ...)` 的参数 `fields` 覆盖了导入的模块，下一行 `fields.Domain.AND` 会访问传入的列表。已隔离复现 `AttributeError`。这是调用该兼容接口时的确定错误，不表示当前使用新接口的每张图表都会报错。
- 趋势图使用 `temperature:avg` / `humidity:avg`，探头统计则按 `sample_count` 加权。聚合跨越不同样本量的日/小时汇总时，会产生“均值的均值”，与统计卡片不一致。
- 建议：统一后端统计服务及加权规则，同时覆盖新旧聚合接口、图表、透视和导出；对汇总数据不再把行数当样本数。
- 验收：100 个 10 度样本和 1 个 20 度样本，在卡片、图表、透视和导出中都约为 10.099，而不是 15。

### 9. 高优先级待集成验证：公开控制入口的权限边界

- 位置：`models/iot_device.py:619`；`security/iot_security.xml:20`。
- `action_delay_toggle` 在入口直接遍历 `self.sudo()`，下游发布路径没有看到对原调用者角色和记录所属公司的检查。记录规则存在，但不应依赖已提升权限的记录集来执行隔离。
- 源码层面需要优先补强；本次没有使用其他公司账号调用 RPC，也没有证实生产环境的可利用性。
- 建议：所有公开控制、绑定、批量操作在提升权限前验证角色和原记录集访问范围；将“查看”“操作”“配置管理”分开授权，再仅对必要的内部写入提升权限。
- 验收：在 `imytestlan` 使用模拟发布器和两家公司测试账号，验证未授权、只读、跨公司和批量混入记录都不能发送设备命令。

## 展示、性能与维护优化

| 范围 | 当前证据或限制 | 建议 |
| --- | --- | --- |
| 趋势图完整性 | `static/src/js/th_reading_graph_lock.js:171` 只取最新 10000 条，未提示截断；多探头共享此上限。 | 显示请求区间、实际覆盖区间、样本数和截断提示；按区间自适应聚合，降采样保留异常峰值和缺测区间。 |
| 探头名称 | 已有位置友好名称和保留自定义名称的回归测试，不是仍未实现。 | 在列表、图例、告警和导出统一显示“位置 / 名称”，技术编号作为副信息；增加按房间/设备保存的常用视图。 |
| 监测体验 | 当前主要是读数和趋势；`models/th_alert.py` 的告警状态只有 open/closed。 | 增加上下限带、异常持续时长、采样新鲜度、断点说明、告警确认/恢复/处理记录；阈值增加持续时间与回差，减少抖动告警。 |
| 时间边界 | 日汇总使用 `date_trunc('day', reported_at)`；`models/iot_attendance_punch.py:93` 使用执行用户上下文时区判断同日。 | UTC 存储，按公司业务时区确定本地日边界，明确展示时区；跨午夜班次应由排班和业务规则决定，不能简单等同于过期打卡。需先确认业务规则。 |
| 历史可追溯性 | `models/th_reading.py:19` 起的公司和位置字段关联当前探头，重新绑定会改变历史记录的归属/位置展示。 | 分开存储“采样时公司和位置快照”与“当前探头位置”；关键点位利用现有 `keep_full_history`，保留期按公司策略设定。不要未经数据保留决策就以均值替代全部原始数据。 |
| 升级性能 | `models/th_reading.py:86` 的 `init()` 包含全量计数重建与去重处理。 | 一次性修复放入版本迁移；日常升级避免重复全表清洗；先用真实体量副本测升级耗时、锁等待和回滚时间。 |
| 高频写入 | 逐探头创建/更新，以及同一网关、探头热点行争用。 | 先记录 SQL 次数、延迟和锁等待，再做批量查找/写入、缩短事务、聚合最新状态刷新。不要只提高重试次数掩盖争用。 |
| OpenWrt | `middleware/iot_bridge/src/main.rs:1327` 逐 AP 顺序等待 SSH 和回写，再等待下一周期。 | 使用有上限的并发和每台设备独立截止时间；一台不可达不拖延所有 AP。缓存显示需标明采集时间。 |
| 健康与运维 | `middleware/iot_bridge/src/main.rs:372` 的健康接口固定返回成功，只能证明进程响应。 | 保留简单存活检查，另加就绪/诊断状态：MQTT、Odoo、队列、磁盘、最后成功入库；汇总重试与拒绝原因，日志脱敏。 |
| 翻译 | 737 个词条检查通过，但 `models/iot_attendance_punch.py:127` 等直接保存英文消息。 | 利用已有 `error_code` 在展示时按语言翻译，保留原始技术详情；补运行时错误、通知、报表与空状态覆盖。词条完整不等于语言自然度验收。 |
| 测试与发布 | 现有 22 个 Python 测试方法，没有发现 Rust 单元测试标记。 | 补失败注入、补传/回放、晚到汇总、租户隔离、配置 ACK 和模拟固件状态机测试；测试服务器验收通过后才备份、灰度和部署。 |

## 分阶段落地

1. 数据与控制安全：优先处理入库确认、持久队列、事件幂等、租户身份和权限、汇总合并，以及明确关闭路径。身份和汇总变更必须先备份、统计待迁移数据，不能只更新代码。
2. 公司配置与统计一致性：增加完整配置 ACK 和重新绑定同步；统一时间边界、统计聚合和历史位置快照；修复 `read_group`。
3. 使用体验与扩展能力：优化图表、告警、翻译、AP 调度、数据库热点与诊断指标。以实际数据量和故障场景做基准，不预先承诺提升倍数。

测试默认使用 `imytestlan`，隔离真实设备命令、外发通知与其他生产集成。生产发布需单独进行当前状态核验、备份和回滚演练，本次没有执行。

## 本次验证结果

- 六个最小复现成立：兼容聚合接口异常、二进制入库异常被吞、二进制重放时间变化、探头身份碰撞改写网关、不完整网络配置确认、晚到数据覆盖日汇总。
- 前五项通过实际 Python 方法的 AST 隔离执行，使用假的时钟/ORM/发布边界；第六项执行实际 SQL 的 SQLite 方言适配版本。它们不是完整 Odoo/PostgreSQL 集成测试。
- Python AST 解析 50 个文件通过；XML 解析 36 个文件通过；JavaScript `node --check` 2 个文件通过。
- `python tools/check_i18n.py` 通过：`source_terms=737`，4 个目录。
- `cargo check --locked --offline` 通过。仅是当前 Windows 环境编译检查，不是 Linux 部署或设备运行验证。
- 本地缺少 Odoo 运行时，因此没有重新执行现有 22 个 Odoo 测试；没有进行浏览器全界面、多语言人工验收或固件硬件实测。
- 仅新增本审查文件；业务源码、配置、固件和服务器未变更。
